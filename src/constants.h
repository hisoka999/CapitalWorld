#pragma once

enum class WorldSize : int
{
    Small = 1,
    Medium = 5,
    Big = 10,
    Huge = 20,
};

enum class Difficulty
{
    Easy = 4,
    Normal = 2,
    Hard = 1,
};

enum class CityNames
{
    de,
    gb
};